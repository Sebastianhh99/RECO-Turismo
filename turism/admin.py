from turism.models import Place, TravelDate, Weather
from django.contrib import admin

# Register your models here.

admin.site.register(Weather)
admin.site.register(Place)
admin.site.register(TravelDate)
